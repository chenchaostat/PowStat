% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte_logrank.R
\name{ps_tte_logrank}
\alias{ps_tte_logrank}
\title{Time-to-Event Two-Arm Logrank Design}
\usage{
ps_tte_logrank(
  medsurv_control,
  hr,
  n_interim = 0,
  info_frac = NULL,
  alpha = 0.025,
  beta = 0.1,
  allocation_ratio = 1,
  accrual_rate = NULL,
  accrual_duration = NULL,
  n_total = NULL,
  total_study_duration = NULL,
  dropout_rate_control = 0,
  dropout_rate_treatment = dropout_rate_control,
  spending_function = "sfLDOF",
  sided = 1,
  digits = 6
)
}
\arguments{
\item{medsurv_control}{Numeric. Median survival in the control group (months).}

\item{hr}{Numeric. Hazard ratio (treatment/control). Must be != 1.}

\item{n_interim}{Integer. Number of interim analyses. 0 for fixed design.}

\item{info_frac}{Numeric vector or NULL. Information fractions for interim looks.}

\item{alpha}{Numeric. One-sided significance level. Default 0.025.}

\item{beta}{Numeric. Type II error rate. Default 0.1.}

\item{allocation_ratio}{Numeric. Treatment/control allocation ratio. Default 1.}

\item{accrual_rate}{Numeric or NULL. Accrual rate per month (Mode 1 or 2).}

\item{accrual_duration}{Numeric or NULL. Accrual duration in months (Mode 1 or 3).}

\item{n_total}{Integer or NULL. Total sample size (Mode 2).}

\item{total_study_duration}{Numeric or NULL. Total study duration in months (Mode 3).}

\item{dropout_rate_control}{Numeric. Annual dropout rate in control. Default 0.}

\item{dropout_rate_treatment}{Numeric. Annual dropout rate in treatment. Default same as control.}

\item{spending_function}{Character. Spending function name. Default "sfLDOF".}

\item{sided}{Integer. 1 or 2. Default 1.}

\item{digits}{Integer. Decimal places. Default 6.}
}
\value{
An object of class \code{PowStatTTELogrank} containing:
\describe{
\item{input}{Study parameters}
\item{model_summary}{Survival and dropout model}
\item{sample_size_summary}{Sample size by group}
\item{event_summary}{Event and information summary}
\item{duration_summary}{Accrual and study duration}
\item{stages}{Analysis-look details with boundaries}
}
}
\description{
Calculate sample size, event targets, calendar duration, and efficacy
boundaries for a two-arm logrank test design. Supports fixed designs
and group sequential designs (GSD) with three input modes.
}
\details{
Three input modes:
\itemize{
\item Mode 1: \code{accrual_rate} + \code{accrual_duration} -> solve follow-up duration
\item Mode 2: \code{accrual_rate} + \code{n_total} -> solve accrual duration then follow-up
\item Mode 3: \code{accrual_duration} + \code{total_study_duration} -> solve sample size
}
}
\examples{
\dontrun{
# Fixed design - Mode 1
res <- ps_tte_logrank(
  medsurv_control = 9.7,
  hr = 0.692,
  n_interim = 0,
  alpha = 0.025,
  beta = 0.15,
  accrual_rate = 20,
  accrual_duration = 21.5,
  dropout_rate_control = 0.10
)
print(res)

# GSD - Mode 1
res_gsd <- ps_tte_logrank(
  medsurv_control = 9.7,
  hr = 0.692,
  n_interim = 1,
  info_frac = 0.7,
  alpha = 0.025,
  beta = 0.15,
  accrual_rate = 20,
  accrual_duration = 21.75,
  dropout_rate_control = 0.10,
  spending_function = "sfLDOF"
)
print(res_gsd)
}
}
